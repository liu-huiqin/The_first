<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!--mybatis映射文件位置必须要和dao接口的包结构相同-->
<mapper namespace="com.ssm.dao.IUserDao"><!--这个要注意namespace-->
    <select id="findAll" resultType="com.ssm.domain.User"> <!--这个id属性必须要和dao接口里面的方法名相同-->
       select * from userinfo
    </select>
</mapper>
